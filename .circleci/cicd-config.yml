# ================================================================
# 📁 File: .circleci/cicd-config.yml
# 📘 CircleCI Configuration — Laravel + Docker + Smart Deploy
# ================================================================
version: 2.1

# ================================================================
# 🧩 REUSABLE COMMANDS SECTION
# ================================================================
commands:

  # ---------------------------------------------------------------
  # 🔐 DOCKER LOGIN
  # ---------------------------------------------------------------
  docker-login:
    description: "Login to DockerHub using environment credentials"
    steps:
      - run:
          name: DockerHub Login
          command: |
            echo "🔐 Logging in to DockerHub ..."

            if [ -z "$DOCKERHUB_USERNAME" ] || [ -z "$DOCKERHUB_TOKEN" ]; then
              echo "❌ DOCKERHUB_USERNAME or DOCKERHUB_TOKEN is not set."
              exit 1
            fi

            if echo "$DOCKERHUB_TOKEN" | docker login -u "$DOCKERHUB_USERNAME" --password-stdin; then
              echo "✅ Successfully logged in to DockerHub as ${DOCKERHUB_USERNAME:0:2}***"
            else
              echo "❌ Failed to log in to DockerHub"
              exit 1
            fi

  # ---------------------------------------------------------------
  # 🏗️ DOCKER BUILD
  # ---------------------------------------------------------------
  build-image:
    description: "Build Docker image for Yotta Dashboard"
    steps:
      - run:
          name: Build Docker Image
          command: |
            echo "🚀 Building Docker image..."
            docker build -t yotta-dashboard:latest -f dockerfile .
            echo "✅ Docker image built successfully."
            docker images -a

  # ---------------------------------------------------------------
  # ⚙️ LARAVEL OPTIMIZATION
  # ---------------------------------------------------------------
  laravel-optimize:
    description: "Clear and cache Laravel configuration for optimization"
    steps:
      - run:
          name: Laravel Optimize
          command: |
            echo "⚙️ Optimizing Laravel application..."

            # Clear existing caches
            docker exec yotta-dashboard php artisan config:clear || echo "⚠️ Failed config:clear"
            docker exec yotta-dashboard php artisan cache:clear || echo "⚠️ Failed cache:clear"
            docker exec yotta-dashboard php artisan route:clear || echo "⚠️ Failed route:clear"
            docker exec yotta-dashboard php artisan view:clear || echo "⚠️ Failed view:clear"

            # Rebuild caches
            docker exec yotta-dashboard php artisan config:cache || echo "⚠️ Failed config:cache"
            docker exec yotta-dashboard php artisan route:cache || echo "⚠️ Failed route:cache"
            docker exec yotta-dashboard php artisan view:cache || echo "⚠️ Failed view:cache"

            echo "✅ Laravel caching complete."

            # Display Laravel about information
            echo "Displaying Laravel about information..."
            docker exec yotta-dashboard php artisan about || echo "⚠️ Failed to display Laravel about"


            echo "✅ Laravel optimization and caching completed."

  # ---------------------------------------------------------------
  # 🗄️ LARAVEL MIGRATION (STANDARD)
  # ---------------------------------------------------------------
  laravel-migrate:
    description: "Run Laravel database migrations safely"
    steps:
      - run:
          name: Laravel Migrate
          command: |
            echo "🗄️ Running Laravel database migrations ..."

            # Ensure container is running
            if ! docker ps --format '{{.Names}}' | grep -q '^yotta-dashboard$'; then
              echo "❌ Container yotta-dashboard not running."
              exit 1
            fi

            # Run migration
            if docker exec yotta-dashboard php artisan migrate --force; then
              echo "✅ Laravel migrations completed successfully."
            else
              echo "❌ Laravel migrations failed."
              docker exec yotta-dashboard php artisan migrate:status || echo "⚠️ Could not get migration status."
              exit 1
            fi

  # ---------------------------------------------------------------
  # 🧠 LARAVEL MIGRATION (FALLBACK STRATEGY)
  # ---------------------------------------------------------------
  laravel-migrate-fallback:
    description: "Run Laravel migrations with rollback fallback (for staging)"
    steps:
      - run:
          name: Laravel Migrate with Fallback
          command: |
            CONTAINER="${CONTAINER_NAME:-yotta-dashboard}"
            echo "🧠 Starting Laravel migrations in container '$CONTAINER' at $(date)"

            if ! docker ps --format '{{.Names}}' | grep -q "^$CONTAINER\$"; then
              echo "❌ Container $CONTAINER not running."
              exit 1
            fi

            echo "➡️ Running migration..."
            if docker exec $CONTAINER php artisan migrate --force; then
              echo "✅ Migrations completed successfully."
            else
              echo "⚠️ Migration failed! Attempting rollback + retry..."
              docker exec $CONTAINER php artisan migrate:rollback --force || echo "⚠️ Rollback failed!"
              docker exec $CONTAINER php artisan migrate --force || {
                echo "❌ Migration still failed after retry!"
                docker exec $CONTAINER php artisan migrate:status || true
                exit 1
              }
            fi

            docker exec $CONTAINER php artisan migrate:status || echo "⚠️ Could not fetch migration status."
            echo "📝 Laravel migration process completed at $(date)"

  # ---------------------------------------------------------------
  # 🧾 GENERATE .ENV FILE
  # ---------------------------------------------------------------
  create-env:
    description: "Generate Laravel .env file without generating APP_KEY"
    steps:
      - run:
          name: Generate .env file
          command: |
            set -e
            echo "⚙️ Generating .env file..."

            [ -f .env ] && rm -f .env

            REQUIRED_KEYS=(
              APP_NAME 
              APP_DEBUG 
              APP_ENV 
              APP_URL
              CACHE_DRIVER 
              DB_CONNECTION 
              DB_DATABASE
              DB_HOST 
              DB_USERNAME 
              DB_PASSWORD 
              DB_PORT
              MAIL_FROM_NAME 
              MAIL_MAILER 
              QUEUE_CONNECTION 
              SESSION_DRIVER
            )

            for key in "${REQUIRED_KEYS[@]}"; do
              value=${!key:-}
              if [ -z "$value" ]; then
                echo "❌ Missing required environment variable: $key"
                exit 1
              fi
              echo "$key=$value" >> .env
            done

            echo "✅ .env file successfully generated."

  # ---------------------------------------------------------------
  # 🔑 GENERATE LARAVEL APP_KEY
  # ---------------------------------------------------------------
  generate-app-key:
    description: "Generate and inject Laravel APP_KEY into container"
    steps:
      - run:
          name: Generate APP_KEY
          command: |
            set -e
            ready=false
            echo "⏳ Waiting for yotta-dashboard container..."

            for i in {1..20}; do
              if docker exec yotta-dashboard php -v >/dev/null 2>&1; then
                echo "✅ Container ready (attempt $i)"
                ready=true
                break
              fi
              sleep 3
            done

            if [ "$ready" != "true" ]; then
              echo "❌ Container never became ready."
              docker logs yotta-dashboard || true
              exit 1
            fi

            echo "🔑 Generating Laravel APP_KEY..."
            KEY=$(docker exec yotta-dashboard php artisan key:generate --show)
            echo "APP_KEY=$KEY" >> .env
            docker cp .env yotta-dashboard:/var/www/.env

            docker exec yotta-dashboard php artisan config:clear
            docker exec yotta-dashboard php artisan config:cache
            echo "✅ APP_KEY successfully generated and applied."

# ================================================================
# 🚀 JOBS SECTION
# ================================================================
jobs:

  # ---------------------------------------------------------------
  # ✅ JOB 1: TESTING BUILD & ENV VALIDATION
  # ---------------------------------------------------------------
  Testing:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true

      - create-env

      - run:
          name: Run Docker Compose
          command: |
            echo "🚀 Starting MySQL & Dashboard containers..."
            docker compose up -d --build mysql
            echo "✅ MySQL started successfully."
            docker ps -a

            echo "🚀 Starting Dashboard container..."
            docker compose up -d --build dashboard
            echo "✅ Dashboard started successfully."
            docker ps -a

      - run:
          name: Wait for MySQL readiness
          command: |
            echo "⏳ Waiting for MySQL..."
            for i in {1..20}; do
              docker exec yotta-mysql sh -c 'mysql -u yotta -pyotta_pass -e "SELECT 1;"' && break
              echo "Still waiting..."
              sleep 2
            done
            echo "✅ MySQL ready!"

      - run:
          name: php artisan package:discover
          command: docker exec yotta-dashboard php artisan package:discover

      - generate-app-key
      - laravel-optimize
      - laravel-migrate

      - run:
          name: Health Check Laravel
          command: |
            success=false
            for i in {1..30}; do
              RESPONSE=$(docker exec yotta-dashboard curl -s -w " %{http_code}" http://127.0.0.1:8002/health || echo "000")
              BODY=$(echo "$RESPONSE" | sed 's/ [0-9]\{3\}$//')
              CODE=$(echo "$RESPONSE" | grep -oE '[0-9]{3}$')

              if [ "$CODE" = "200" ]; then
                echo "✅ /health OK on attempt $i (body: $BODY)"
                success=true
                break
              else
                echo "Waiting for Laravel... ($CODE, body: $BODY)"
                sleep 3
              fi
            done

            if [ "$success" = false ]; then
              echo "❌ Health check failed after 30 attempts"
              docker logs yotta-dashboard || true
              docker exec yotta-dashboard tail -n 50 storage/logs/laravel.log || true
              exit 1
            fi

  # ---------------------------------------------------------------
  # 🐳 JOB 2: PUSH DOCKER IMAGE TO HUB
  # ---------------------------------------------------------------
  Push-To-Docker:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - setup_remote_docker:
          docker_layer_caching: true

      - docker-login

      - run:
          name: Build image with version tag + latest
          command: |
            git fetch --tags
            TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "latest")
            IMAGE_TAG=$(echo ${TAG} | tr '/:*' '-' | tr '[:upper:]' '[:lower:]')
            docker build -t ${DOCKERHUB_REPO}:$IMAGE_TAG -t ${DOCKERHUB_REPO}:latest -f dockerfile .
            docker images -a | grep ${DOCKERHUB_REPO} | grep $IMAGE_TAG
            echo "export IMAGE_TAG=$IMAGE_TAG" >> $BASH_ENV

      - run:
          name: Push image to DockerHub
          command: |
            docker push ${DOCKERHUB_REPO}:$IMAGE_TAG
            docker push ${DOCKERHUB_REPO}:latest
            echo "✅ Image pushed to DockerHub successfully."

  # ---------------------------------------------------------------
  # 🚀 JOB 3: DEPLOY TO STAGING (SMART DEPLOY + LOGGING)
  # ---------------------------------------------------------------
  Deploy-Staging:
    docker:
      - image: cimg/base:stable

    steps:
      - add_ssh_keys:
          fingerprints:
            - "SHA256:B0nVmApbymFWeHvPvtf1dWL0GFbsKiNrijvoITT7VH0"

      - checkout

      - run:
          name: Deploy to Staging (Auto Smart Deploy + Logging)
          command: |
            ssh -o StrictHostKeyChecking=no $SERVER_USER@$SERVER_IP "
              set -e
              
              # ======================================================
              # 📜 LOGGING SETUP
              # ======================================================
              BASE_DIR=/home/ubuntu/dev-yotta
              LOG_DIR=\$BASE_DIR/LogsCICDStaging
              TIMESTAMP=\$(date '+%Y%m%d-%H%M%S')
              LOG_PATH=\$LOG_DIR/deploy-staging-\$TIMESTAMP.log

              # Ensure base dir and permissions
              sudo mkdir -p \$BASE_DIR \$LOG_DIR
              sudo chown -R ubuntu:ubuntu \$BASE_DIR
              sudo chmod -R 775 \$BASE_DIR

              mkdir -p \$LOG_DIR
              touch \$LOG_PATH

              # 🔐 Log Rotation (hapus log >14 hari)
              find \$LOG_DIR -type f -mtime +14 -exec rm -f {} \; || true

              echo '🧾 Logging deployment output to: \$LOG_PATH' | tee -a \$LOG_PATH

              # ======================================================
              # 🧱 PREPARE SERVER DIRECTORIES
              # ======================================================
              echo '📁 Preparing required directories...' | tee -a \$LOG_PATH

              REQUIRED_DIRS=(
                \"\$BASE_DIR/html\"
                \"\$BASE_DIR/html/bootstrap/cache\"
                \"\$BASE_DIR/html/storage/app\"
                \"\$BASE_DIR/html/storage/framework/cache/data\"
                \"\$BASE_DIR/html/storage/framework/cache/laravel-excel\"
                \"\$BASE_DIR/html/storage/framework/sessions\"
                \"\$BASE_DIR/html/storage/framework/views\"
                \"\$BASE_DIR/html/storage/logs\"
                \"\$BASE_DIR/storage\"
                \"\$BASE_DIR/LogsCICDStaging\"
              )

              for dir in \${REQUIRED_DIRS[@]}; do
                if [ ! -d \"\$dir\" ]; then
                  echo \"📂 Creating missing directory: \$dir\" | tee -a \$LOG_PATH
                  mkdir -p \"\$dir\"
                else
                  echo \"✅ Directory exists: \$dir\" | tee -a \$LOG_PATH
                fi
              done

              # Fix ownership & permissions for Laravel folders
              echo '🔒 Fixing Laravel folder permissions...' | tee -a \$LOG_PATH
              sudo chown -R ubuntu:www-data \$BASE_DIR/html/storage \$BASE_DIR/html/bootstrap/cache
              sudo chmod -R 775 \$BASE_DIR/html/storage \$BASE_DIR/html/bootstrap/cache

              echo '✅ All required directories ready.' | tee -a \$LOG_PATH

              # ======================================================
              # 🧠 CHECK RUNNING CONTAINERS
              # ======================================================
              echo '🔍 Checking existing Docker services...' | tee -a \$LOG_PATH
              RUNNING_SERVICES=\$(docker compose ps --services --status running || true)

              if echo \"\$RUNNING_SERVICES\" | grep -q 'dashboard'; then
                echo '🔄 Existing dashboard container found — updating image...' | tee -a \$LOG_PATH
                docker pull \$DOCKERHUB_REPO:latest | tee -a \$LOG_PATH
                docker compose stop dashboard | tee -a \$LOG_PATH
                docker compose rm -f dashboard | tee -a \$LOG_PATH
              else
                echo '🆕 No existing dashboard container — running fresh setup...' | tee -a \$LOG_PATH
                docker pull \$DOCKERHUB_REPO:latest | tee -a \$LOG_PATH
              fi

              # ======================================================
              # 🚀 START NEW CONTAINER
              # ======================================================
              echo '🚀 Starting dashboard container...' | tee -a \$LOG_PATH
              docker compose up -d dashboard --remove-orphans | tee -a \$LOG_PATH
              DASHBOARD_CID=\$(docker compose ps -q dashboard)

              # ======================================================
              # ⚙️ INITIAL LARAVEL SETUP
              # ======================================================
              echo '📦 Copying .env into container...' | tee -a \$LOG_PATH
              docker cp ./.env \$DASHBOARD_CID:/var/www/.env || true

              echo '🔧 Running php artisan package:discover...' | tee -a \$LOG_PATH
              docker exec \$DASHBOARD_CID php artisan package:discover | tee -a \$LOG_PATH

              echo '⚙️ Optimizing Laravel configuration...' | tee -a \$LOG_PATH
              make dy-laravel-optimize-all | tee -a \$LOG_PATH

              # ======================================================
              # 🧠 SMART MIGRATION CHECK
              # ======================================================
              echo '🧠 Checking for pending migrations...' | tee -a \$LOG_PATH

              FS_MIGRATIONS=\$(docker exec \$DASHBOARD_CID ls -1 database/migrations | sort)
              DB_MIGRATIONS=\$(docker exec \$DASHBOARD_CID php artisan migrate:status --no-interaction \
                | grep '\[.*Ran.*\]' \
                | awk '{print \$1}' \
                | sort || true)

              DIFFERENCE=\$(comm -23 <(echo \"\$FS_MIGRATIONS\") <(echo \"\$DB_MIGRATIONS\") || true)

              if [ -n \"\$DIFFERENCE\" ]; then
                echo '🆕 Found new migrations:' | tee -a \$LOG_PATH
                echo \"\$DIFFERENCE\" | tee -a \$LOG_PATH
                echo '➡️ Running php artisan migrate --force ...' | tee -a \$LOG_PATH

                if docker exec \$DASHBOARD_CID php artisan migrate --force | tee -a \$LOG_PATH; then
                  echo '✅ Migrations applied successfully.' | tee -a \$LOG_PATH
                else
                  echo '⚠️ Migration failed! Rolling back and retrying...' | tee -a \$LOG_PATH
                  docker exec \$DASHBOARD_CID php artisan migrate:rollback --force | tee -a \$LOG_PATH || true
                  docker exec \$DASHBOARD_CID php artisan migrate --force | tee -a \$LOG_PATH || {
                    echo '❌ Migration retry failed. Check logs.' | tee -a \$LOG_PATH
                    docker exec \$DASHBOARD_CID tail -n 50 /var/www/storage/logs/laravel.log | tee -a \$LOG_PATH
                    exit 1
                  }
                fi
              else
                echo '✅ No new migrations found. Skipping migrate.' | tee -a \$LOG_PATH
              fi

              # ======================================================
              # 🌐 AUTO SERVER CONTEXT CONFIG
              # ======================================================
              echo '🔍 Adjusting .env automatically for current server...' | tee -a \$LOG_PATH
              PUBLIC_IP=\$(curl -s https://api.ipify.org || echo '127.0.0.1')
              HOSTNAME=\$(hostname)

              docker exec \$DASHBOARD_CID sh -c \"
                sed -i 's|APP_URL=.*|APP_URL=https://\$PUBLIC_IP|g' /var/www/.env
                sed -i 's|APP_NAME=.*|APP_NAME=YOTTA-\$(echo \$HOSTNAME | tr '[:lower:]' '[:upper:]')|g' /var/www/.env
                php artisan config:clear
                php artisan config:cache
              \" | tee -a \$LOG_PATH

              echo '✅ Server environment adjusted automatically.' | tee -a \$LOG_PATH

              # ======================================================
              # 🩺 HEALTH CHECK
              # ======================================================
              echo '🌐 Performing health check...' | tee -a \$LOG_PATH
              APP_URL=\$(docker exec \$DASHBOARD_CID sh -c \"grep ^APP_URL= /var/www/.env | cut -d '=' -f2\")
              for i in {1..20}; do
                STATUS=\$(curl -k -s -o /dev/null -w '%{http_code}' \${APP_URL}/health || echo 000)
                if [ \"\$STATUS\" = \"200\" ]; then
                  echo \"✅ Health check OK (\$APP_URL, HTTP 200)\" | tee -a \$LOG_PATH
                  break
                fi
                echo \"Waiting for Laravel... (status: \$STATUS)\" | tee -a \$LOG_PATH
                sleep 5
              done

              echo '✅ Deployment to STAGING completed successfully!' | tee -a \$LOG_PATH
            "


# ================================================================
# 🔄 WORKFLOW PIPELINE ORDER
# ================================================================
workflows:
  version: 2
  ci-cd:
    jobs:
      - Testing:
          context:
            - dockerhub
            - laravel_app
          filters:
            branches:
              only: development
            tags:
              ignore: /.*/

      - Push-To-Docker:
          context:
            - dockerhub
          requires:
            - Testing
          filters:
            branches:
              only: development
            tags:
              only: /^v[0-9]+\.[0-9]+\.[0-9]+$/

      - Deploy-Staging:
          context:
            - deploy_server
            - dockerhub
          requires:
            - Push-To-Docker
          filters:
            branches:
              only: development
            tags:
              only: /^v[0-9]+\.[0-9]+\.[0-9]+$/

# ================================================================
# 📘 IMPLEMENTATION NOTES
# ================================================================
# ✅ Pipeline Flow:
#    1. Testing → Build and validate Laravel app
#    2. Push    → Push built image to DockerHub
#    3. Deploy  → Smart deploy to Staging server
#
# ⚙️ Deployment Flow:
#    - Start database container first
#    - Start dashboard container
#    - Run php artisan package:discover
#    - Optimize and cache Laravel configs
#
# 🧠 Smart Migration:
#    - Run migrations only if new migration files exist
#    - Safe for repeated deploys
#
# 🌐 Auto Server Configuration:
#    - Automatically updates APP_URL and APP_NAME per server
# ================================================================